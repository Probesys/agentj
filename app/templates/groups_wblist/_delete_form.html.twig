<form method="post" action="{{ path('groups_wblist_delete', {'groupId': groups_wblist.groupId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groups_wblist.groupId) }}">
    <button class="btn">Delete</button>
</form>
