<form class="d-inline" action="{{ path('homepage', {'_switch_user': user.email }) }}" method="post">
    <button type="submit" class="btn btn-sm btn-primary" title="{{ 'Generics.actions.connectAs'|trans }} {{ user.email }}">
        {{ 'Generics.actions.impersonate'|trans }}
    </button>
</form>
