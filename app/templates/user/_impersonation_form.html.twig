<form class="d-inline" action="{{ path('homepage', {'_switch_user': user.getEmailFromRessource() }) }}" method="post">
    <button type="submit" class="btn btn-sm btn-primary" title="{{ 'Generics.actions.connectAs'|trans }} {{ user.emailFromResource }}">
        <i class="fas fa-random d-none d-md-block d-lg-none"></i>
        <span class="d-md-none d-lg-block">{{ 'Generics.actions.impersonate'|trans }}</span>
    </button>
</form>